version: 2

models:
  - name: PIT_LAST_PART
    description: "PIT containing the last available information per part per satellite"

    config:
      access: public
      materialized: view
      contract:
        enforced: true

    columns:
      - name: HUB_ID_PART
        data_type: VARCHAR
        description: "Hub id for part"
        data_tests:
          - unique
          - not_null

      - name: DT_LOAD
        data_type: TIMESTAMP_NTZ
        description: "Hub id load timestamp"

      - name: SAT_STATIC_DT_LOAD
        data_type: TIMESTAMP_NTZ
        description: "Latest load timestamp from SAT_PART_STATIC"

      - name: SAT_MAIN_DT_LOAD
        data_type: TIMESTAMP_NTZ
        description: "Latest load timestamp from SAT_PART_MAIN"

      - name: TENANT
        data_type: VARCHAR
        description: "Tenant information"
